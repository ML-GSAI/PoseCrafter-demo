第一列：用于 PoseCrafter 训练的训练帧。

第二列：测试帧，这些帧与训练帧不同，将从中提取姿势。这些测试帧未用于 PoseCrafter 的训练。

第三列：从测试帧中提取的派生姿势。

第四列：由 PoseCrafter 生成的帧，这些帧由测试帧的派生姿势引导。


视频1到6使用的是同一个人的pose进行引导，其中视频6修改了文本使人物的头发和胡须变色。
视频7到9使用的是人工设计的pose，这些pose并不存在于训练帧。
视频10到视频15具有复杂的动作，其中视频11和12使用的是同一个的pose，其余视频使用的是其他人的pose。